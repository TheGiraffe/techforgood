import { useEffect, useState } from 'react';
import { useAuth } from '../../features/firebase/AuthProvider';

const UserBids = () => {
    
    return (
        <div>
            <h1>User Bids</h1>
            <p>This page will display the bids made by the user on various requests.</p>
            <p>yada yada hello world</p>
        </div>
    );
}

export default UserBids;